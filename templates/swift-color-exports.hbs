//
//  Colors.swift
//  Bright
//
//  Generated by Bagheera on {{dateFormat (dateNow)}}.
//
import SwiftUI

extension Color {

{{#each properties as |property|}}
{{#each property as |category|}}
  {{#if category.value}}
  static let {{category.name}} = Color("{{category.name}}")
  {{else}}
  {{#each category as |item|}}
  {{#if item.value}}
  static let {{item.name}} = Color("{{item.name}}")
  {{else}}
  {{#each item as |subitem|}}
  {{#if subitem.value}}
  static let {{subitem.name}} = Color("{{subitem.name}}")
  {{else}}
  {{#each subitem as |subentity|}}
  static let {{subentity.name}} = Color("{{subentity.name}}")
  {{/each}}
  {{/if}}
  {{/each}}
  {{/if}}
  {{/each}}
  {{/if}}
{{/each}}
{{/each}}

  // Additional
  static let spinnerIndicator = Color("spinnerIndicator")
  static let spinnerTracker = Color("spinnerTracker")
  static let contentBlackInvertable = Color("contentBlackInvertable")
  static let contentSubduedStatic = Color("contentSubduedStatic")
  static let contentDefaultStatic = Color("contentDefaultStatic")

}

struct Colors {

  static let mustard = PlatformColor(normalRed: 239, green: 181, blue: 41, alpha: 1)
  static let neon = PlatformColor(normalRed: 223, green: 252, blue: 42)
  static let darkBackgroundBlue = PlatformColor(normalRed: 36, green: 33, blue: 58)
  static let controlButtonRed = PlatformColor(normalRed: 255, green: 82, blue: 82)
  static let controlButtonGreen = PlatformColor(normalRed: 0, green: 200, blue: 83)
  static let grey50 = PlatformColor(normalRed: 240, green: 241, blue: 244)
  static let grey100 = PlatformColor(normalRed: 231, green: 233, blue: 237)
  static let grey200 = PlatformColor(normalRed: 198, green: 201, blue: 208)
  static let grey300 = PlatformColor(normalRed: 173, green: 176, blue: 188)
  static let grey400 = PlatformColor(normalRed: 111, green: 113, blue: 135)
  static let grey500 = PlatformColor(normalRed: 82, green: 82, blue: 108)
  static let grey600 = PlatformColor(normalRed: 61, green: 60, blue: 86)
  static let grey700 = PlatformColor(normalRed: 45, green: 43, blue: 63)
  static let grey800 = PlatformColor(normalRed: 33, green: 31, blue: 47)
  static let gray900 = PlatformColor(normalRed: 33, green: 31, blue: 48)
  static let indigo700 = PlatformColor(normalRed: 73, green: 81, blue: 255)

  static let actionPrimary = PlatformColor(normalRed: 18, green: 16, blue: 25)
  static let backgroundBlack = PlatformColor(normalRed: 18, green: 16, blue: 25)

}

#if iOS
class PlatformColor: UIColor {
    convenience init(normalRed red: CGFloat, green: CGFloat, blue: CGFloat, alpha: CGFloat = 1) {
        self.init(red: red / 255, green: green / 255, blue: blue / 255, alpha: alpha)
    }
}
#endif
#if macOS
class PlatformColor: NSColor {
    convenience init(normalRed red: CGFloat, green: CGFloat, blue: CGFloat, alpha: CGFloat = 1) {
        self.init(Color(.sRGB, red: red / 255, green: green / 255, blue: blue / 255, opacity: alpha))
    }
}
#endif